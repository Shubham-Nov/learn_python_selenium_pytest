🧪 tests/ (Contains Test Scripts)    # This folder contains all test cases, where you write test scripts using 
    frameworks like pytest or unittest.

📄 pages/ (Page Object Model - POM)  # This folder contains Page Object Model (POM) classes, 
    which help separate UI interactions from test scripts.

🛠 utils/ (Utility Functions)    # Contains reusable helper functions, like opening/closing the browser.

⚙ config/ (Configuration Files) # This folder contains settings like test environment URLs, credentials(usename, password), etc.